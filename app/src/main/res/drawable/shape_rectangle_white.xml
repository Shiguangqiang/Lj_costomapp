<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充的颜色 -->
    <solid android:color="@color/F4" />

    <!-- 设置矩形的四个角为弧形 -->
    <corners android:radius="10dip"
        />
    <size
        android:width="80dp"
        android:height="20dp" />
    <stroke
        android:width="1dp"
        android:color="#999"/>

</shape>